# Server Configuration
PORT=3000
NODE_ENV=development

# Frontend URL for CORS (production URL in production)
FRONTEND_URL=http://localhost:5173

# Cache Configuration (optional)
CACHE_FILE_PATH=./cache/weather-cache.json
